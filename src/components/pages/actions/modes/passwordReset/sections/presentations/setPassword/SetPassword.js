import { Typography } from 'src/components'
import { intl } from 'src/i18n'
import './styles.scss'

export const SetPassword = () => {
	const styles = {
		root: 'set-password-presentation'
	}

	return (
		<div className={styles.root}>
			<Typography h5></Typography>
		</div>
	)
}
