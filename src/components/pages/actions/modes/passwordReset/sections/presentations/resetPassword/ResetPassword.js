import { Typography, Button, Page, TextInput } from 'src/components'
import { intl } from 'src/i18n'
import './styles.scss'

export const ResetPassword = () => {
	const styles = {
		root: 'reset-password-presentation'
	}
	return <div className={styles.root}>ResetPassword</div>
}
