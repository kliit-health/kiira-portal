import { createMuiTheme } from '@material-ui/core/styles'

export const theme = createMuiTheme({
	typography: {
		fontWeight: 500,
		fontSize: 14
	},
	palette: {
		primary: {
			main: '#0089FF'
		},
		secondary: {
			main: '#19857b'
		},
		background: {
			default: '#fff'
		}
	}
})
